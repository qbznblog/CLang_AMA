/* Copies a file */
#include <stdio.h>
#include <stdlib.h

int main(int argc, char *argv[])
{
    FILE *source_fp, *dest_fp;
    int ch;

    if (argc != 3) {
        fprintf(stderr, "usage: fcopy source dest\n");
        exit (EXIT_FAILURE);
    }

    if (argc != 3) {

    }
}